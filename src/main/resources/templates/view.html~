<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Bike Comparator</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">

<!-- Encabezado -->
<header class="bg-blue-600 shadow">
    <div class="max-w-7xl mx-auto px-4 py-6 flex justify-center">
        <a href="http://localhost:8080" class="flex items-center">
            <!-- Imagen del logo -->
        </a>
    </div>
</header>

<!-- Contenedor principal -->
<main class="py-12">
    <div class="container mx-auto px-4">
        <!-- Título de la bicicleta -->
        <h1 class="text-4xl font-bold text-center text-gray-800 mb-8" th:text="${bicicleta.getMarcaNombre()} + ' ' + ${bicicleta.getModelo()}"></h1>

        <!-- Imagen de la bicicleta -->
        <div class="flex justify-center mb-6">
            <img class="rounded-lg shadow-lg transition-transform transform hover:scale-105 w-full max-w-xl object-contain"
                 th:src="${bicicleta.getImagenUrl()}"
                 alt="Bicicleta"
                 th:classappend="${bicicleta.getMarcaNombre() == 'Cannondale' ? 'object-fill' : 'object-contain'}">
        </div>


<!--         Especificaciones de componentes -->
        <section class="bg-white p-8 rounded-lg shadow-md max-w-4xl mx-auto">
            <h2 class="text-2xl font-semibold text-gray-700 mb-6 text-center">Especificaciones de Componentes</h2>

            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-2 gap-6 text-gray-800" th:each="componente : ${bicicleta.getComponentes()}">

                <div>
                    <h3 class="font-bold"
                        th:text="${componente.getComponente().getTipo().contains('_') ? componente.getComponente().getTipo().replace('_', ' ') + ':' : componente.getComponente().getTipo() + ':'}">
                    </h3>

                    <p th:text="${componente.getComponente().getDescripcion()}"></p>
                </div>
            </div>
        </section>
        <!-- Enlace a más información -->
        <div class="text-center mt-12">
            <a th:href="${bicicleta.getMasInfoUrl()}" target="_blank" rel="noopener noreferrer"
               class="inline-block bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700 transition">
                Más información
            </a>
        </div>

    </div>
</main>

</body>
</html>
